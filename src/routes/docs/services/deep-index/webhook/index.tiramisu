heading {Reverse API for Deep Index}

Instead of querying for new data on a specific interval, with the Timeleap Reverse
API you can just subscribe to the blockchain events you are interested in and let
our dispatcher notify your HTTP endpoint with the data you requested.

toc {
   introduction,
   start,
   examples,
   schema,
   integrations,
   pricing,
   changes}
